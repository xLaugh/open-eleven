/* Configuration Supabase — À REMPLIR avec ton projet (Supabase → Settings → API).
   Tant que ces deux valeurs sont vides, la connexion est DÉSACTIVÉE et le jeu
   reste 100% invité/local (aucune donnée n'est envoyée). Voir SUPABASE_SETUP.md.
   La clé "anon public" est faite pour être exposée côté client (sécurité via RLS). */
window.SUPABASE_URL = "";       // ex : https://xxxxxxxx.supabase.co
window.SUPABASE_ANON_KEY = "";  // clé "anon public"
